server:
  port: 8080

logging:
  level:
    org.hibernate.sql : debug

spring:
  profiles:
    active: ${YML_STATUS}
  application:
    name: parabole

cloud:
  aws:
    stack:
      auto: false
    
